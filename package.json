{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "pulp:build": "pulp build -- --censor-lib --strict",
    "build": "spago build --purs-args '--censor-lib --strict'",
    "pulp:test": "pulp test",
    "test": "spago test"
  },
  "devDependencies": {
    "purescript": "^0.15.6",
    "purescript-psa": "^0.8.2",
    "rimraf": "^2.5.0",
    "spago": "^0.20.9"
  },
  "dependencies": {
    "pulp": "^16.0.2"
  }
}
